let age; // try assigning a value to tthis variable
const adult =
  age >= 18
    ? `your age is 18 or older You can vote .`
    : age <= 18
    ? `your age is under 18 ,Sorry You can't vote .`
    : `please insert a valid number`;

console.log(adult);
